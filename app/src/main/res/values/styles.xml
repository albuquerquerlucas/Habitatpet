<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/azulEscuro</item>
        <item name="colorPrimaryDark">@color/azulEscuro</item>
        <item name="colorAccent">@color/azulEscuro</item>
        <item name="android:colorBackground">@color/branco_fonte_logo</item>
        <item name="android:textColorPrimary">@color/azulEscuro</item>

        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

</resources>
